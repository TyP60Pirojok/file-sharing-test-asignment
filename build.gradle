plugins {
    id 'java'
    id 'application'
}

application {
    mainClass = 'FileServer'
}

repositories {
    mavenCentral()
}

dependencies {
}

tasks.register('runServer', JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'FileServer'
}